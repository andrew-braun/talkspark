# TalkSpark server instructions

See [AGENTS.md](../../AGENTS.md).

Server-only code in `src/lib/server/`. `OPENAI_API_KEY` via `$env/static/private`. OpenAI Responses API with strict JSON schema in `chat-api.ts`.
